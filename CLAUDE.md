# 작업 규칙

## 푸시 전에 반드시 빌드를 돌린다

```bash
npm ci          # node_modules 가 없으면 한 번만
npm run build   # prebuild(플래시카드·genAnswers) + next build + 타입검사
```

`npm run build` 가 통과하지 않으면 **배포가 통째로 멈춘다.** 화면은 마지막으로
성공한 빌드에 머물러 있고, 그 뒤에 올린 커밋은 하나도 반영되지 않는다.
커밋이 쌓여 있어도 사용자는 옛 화면을 보게 되므로 "왜 안 고쳐졌냐"는 말이
나오기 전까지 아무도 모른다.

실제로 이런 일이 있었다. `src/app/plan/page.tsx` 에서 렌더 조건을
`day.kind === "study"` 에서 `topics.length > 0` 으로 바꿨더니 else 가지의
타입 좁히기가 풀려 `day.note` 접근이 타입 오류가 됐다. 데이터만 고치는
커밋이라고 생각해 빌드를 안 돌렸고, 학습계획 개편 여러 건이 배포되지
않은 채 며칠 묵었다.

데이터(JSON·ts 상수)만 바꾼 커밋도 예외가 아니다. `prebuild` 가 데이터를
읽어 플래시카드와 genAnswers 를 다시 만들기 때문에 데이터가 깨지면 여기서
멈춘다.

## 답안 형식 검사

```bash
python3 scripts/check-answer-format.py <회차>   # 140, ns1-, yc1-30 …
```

규격은 `docs/ANSWER_SPEC.md` 와 `src/lib/prompts.ts` 에 있다.

## 교재 자료를 옮길 때

- 교재 정의에서 색으로 강조된 단어는 채점 키워드다. 29~30자(공백 제외)로
  줄일 때 **그 단어를 빼지 않는다** — 줄이는 대상은 조사·수식어다.
  종결어(기법/알고리즘/다이어그램/제도 …)도 바꾸지 않는다.
  **다른 말로 바꾸지도 않는다.** 「애플리케이션」을 「응용」으로 줄였다가
  MSA 정의가 "응용을 작은 마이크로 서비스로 나눠…"가 되어 무슨 말인지
  알 수 없게 된 적이 있다. 자리가 모자라면 수식어를 버리지 용어를 바꾸지
  않는다. SW·HW·DB·OS 처럼 답안지에서도 그대로 쓰는 약어만 예외다.
  (서브노트 `defShort`·`defPair`·`subDefs` 는 2026-09 에 34~35자 → 29~30자로
  축소했다. 옛 `topicIntros` 정의와 기존 모범답안은 아직 34~35자다.)
- 서브노트 표(`tables`)의 **열 구성은 교재 표를 그대로 따른다** (1~9열).
  교재가 항목만 죽 나열한 목록이면 **1열 표 그대로 둔다** — 「키워드」·「설명」
  열을 지어내지 않는다. 한 번 그렇게 부풀렸다가 교재에 없는 설명이 들어갔다.
  머리글도 교재 그대로다. 보통은 맨 뒤가 「설명」이고 1열이 「구분」,
  가운데가 「구성요소」 같은 꼴이지만 표마다 다르다 — 「유형」·「절차」·
  「핵심 기술」·「장점」·「단점」 무엇이든 교재에 적힌 말을 쓴다.
  - 비교표는 비교 축이 그대로 열이 된다: `[구분 | 3점 추정 | 모수 산정 | 유사 산정]`,
    `[비교항목 | 블랙박스 | 그레이박스 | 화이트박스]`. **이걸 2열(키워드)·3열(설명)로
    접지 않는다.** 한 번 접었다가 「BPI는 해결 가능 / PCE는 해결 불가」 같은
    행렬이 뭉개져 어느 값이 어느 축의 것인지 알 수 없게 된 적이 있다.
    (2026-09 에 3단표로 일괄 변환했다가 165개를 교재 구조로 되돌렸다.)
  - 2열 이후는 1~4줄. 줄은 문자열 안의 `\n`. **한 줄은 공백 제외 5~7칸**
    (영문·숫자는 반 칸으로 센다 — `Factory Method` 가 7칸이다).
    공백이 없는 한 낱말과 영문이 반 넘는 줄(`Bridge, Composite` 같은 이름
    나열)은 12칸까지 둔다.
  - 항목↔설명처럼 짝을 이루는 열은 줄 수를 맞춘다 — 1줄째↔1줄째·2줄째↔2줄째로
    내용이 대응해야 나란히 읽힌다. 한 줄짜리 열은 그 행 전체에 걸리므로
    상관없다 (`[단계 | 목표/산출물 2줄 | 추가 활동 1줄]`). 한 열만 목록인 교재 표
    (`[구분 | 설명 2줄 | 주요 항목 4줄]`)도 있어 검사는 경고만 낸다.
    **한 문장을 칸 너비 때문에 두 줄로 쪼개지 않는다** — 줄은 서로 다른 항목일 때만
    나눈다. 안 들어가면 문장을 줄이지, 끊어서 다음 줄로 넘기지 않는다.
  - 같은 1열(구분)로 행을 반복하는 것보다 **한 행에 줄로 쌓는 쪽**이 낫다 — 화면에
    표를 rowspan 으로 묶지 않아 「프로세스」 같은 구분이 그대로 여러 번 찍힌다.
    (데브옵스 구성 요소가 품질 2행·프로세스 4행·도구 3행이어서 품질 2줄·
     프로세스 4줄·도구 3줄로 접었다. 반복이 5회 이상이면 4줄에 안 들어가므로
     그대로 둔다 — 아직 그런 표가 340개 남아 있다.)
  - 줄 앞에 표시를 붙이지 않는다 (`src/components/TableCell.tsx` 는 줄만 그대로 그린다).
    한때 `①②③` 을 붙여 2열·3열의 ②끼리 짝이라고 표시했는데, 칸 너비(5~7칸) 때문에
    한 문장이 접힌 줄에도 번호가 붙어 「범위 관리 절차 / 방법 정의」가 두 항목처럼
    읽혔다. 번호가 뜻을 끊었다. 데이터에도 번호를 넣지 않는다 — 글자 수에 들어간다.
  - 교재 강조 키워드는 빼지 않는다. 화면은 `whitespace-pre` 로 줄바꿈을 그대로 그린다.
  - 검사: `npx tsx scripts/check-subnote-tables.ts` (빌드 전에 돌린다).
- 커리큘럼(`src/data/curriculum.ts`)의 과목 토픽 목록은 교재 CONTENTS 의
  순서·Priority 를 그대로 따른다. ★=하, ★★=중, ★★★=상.
